# 🚀 نشر فلوسي الخاص على Coolify + حفظ البيانات للأبد

## الفكرة باختصار (البيانات هتتحفظ ازاي؟)

- السيرفر بيحفظ كل حاجة في **ملف واحد**: `/app/data/floosy.db` (SQLite).
- في Coolify هتربط الملف ده بـ **Volume دائم** — يعني حتى لو عملت Redeploy أو حدثت النسخة،
  البيانات (المصاريف + الحسابات + الشركاء) **لا تتمسح أبدًا**.
- النسخ الاحتياطي = نسخة من الملف ده (سطر واحد، مشروح تحت).

## 1) تجهيز الكود (خلصان ✅)

الريبو `hsnshehata/floosy` فيه كل حاجة:
`Dockerfile` + `server/` + الواجهة. Coolify هيبني الصورة من الـ `Dockerfile` تلقائيًا.

## 2) إنشاء السيرفس في Coolify

1. افتح Coolify → مشروعك → **+ New → Application**.
2. المصدر: **GitHub → `hsnshehata/floosy`** → فرع `main`.
3. Build Pack: **Dockerfile** (هيتكشف لوحده).
4. الـ Port: **3000**.
5. دوس **Deploy** مبدئيًا (هيقع أول مرة لعدم وجود متغيرات البيئة — عادي).

## 3) متغيرات البيئة (Environment Variables)

من صفحة التطبيق → **Environment Variables** → ضيف:

| المتغير | القيمة | ملاحظة |
|---|---|---|
| `PORT` | `3000` | ثابت |
| `DATA_DIR` | `/app/data` | مكان ملف القاعدة **داخل الـ Volume** |
| `JWT_SECRET` | (ولّد واحد — الأمر تحت) | مفتاح التوكن — **32 حرف على الأقل** |
| `ADMIN_USERNAME` | `حسن` | حساب المالك — **يتعمل مرة واحدة فقط عند أول تشغيل** |
| `ADMIN_PASSWORD` | `(باسورد قوي من عندك)` | ⚠️ بعد أول دخول غيّرها من الإعدادات |

توليد `JWT_SECRET` قوي (نفذه في PowerShell عندك والصق الناتج):

```powershell
-join ((48..57)+(65..90)+(97..122) | Get-Random -Count 48 | % {[char]$_})
```

> ⚠️ **مهم**: `ADMIN_*` بيستخدم **أول مرة فقط** لإنشاء حسابك. بعد كده تغيير الباسورد
> بيكون من التطبيق (الإعدادات → كلمة المرور). ولو اتسربت كلمة كتبتها في شات —
> غيّرها فورًا بعد الدخول.

## 4) الـ Volume الدائم (أهم خطوة للحفظ)

من صفحة التطبيق → **Storages / Volumes** → **Add**:

- **Source / Volume name**: أي اسم مثل `floosy-data`
- **Destination / Mount path**: `/app/data`

كده ملف `floosy.db` عايش **خارج الكونتينر** — أي تحديث أو redeploy مش هيمسه. ✅

## 5) الدومين + التشغيل

1. من **Domains** ضيف دومينك (مثل `floosy.yourdomain.com`) أو استخدم المؤقت بتاع Coolify.
2. دوس **Deploy / Redeploy**.
3. افتح اللينك → شاشة دخول 🔒 → ادخل بحسابك → **علّم (أو سيب) الحفظ** — التوكن صالح
   **10 سنين** ومحفوظ في `localStorage` على جهازك، يعني **هتفضل داخل للأبد**
   لحد ما تدوس خروج بنفسك.

## 6) إضافة الشركاء والحسابات (من التطبيق نفسه)

- تبويب **👥 الحسابات**:
  - **سلة مشتركة 🧺**: شريكك (مثال: أم محمد) بيشوف مصاريفك وتشوف مصاريفه،
    وتسجلوا انتوا الاتنين في نفس السلة — ضيفه من "إضافة شريك".
  - **حساب منفصل 👤** (بعمله المالك فقط): بيكون له مدير مستقل، والمدير ده
    يقدر يضيف **حتى 5 شركاء** في حسابه هو بس — لا يشوف حسابات غيره ولا يضيف لغيره.
- كل عضو له يوزر وباسورد خاص، وتقدر تصفّر باسورده (🔑) أو تحذفه (×) في أي وقت.

## 7) النسخ الاحتياطي والاسترجاع

**نسخة احتياطية (دقيقة واحدة):**
- من Coolify → التطبيق → **Terminal** ونفذ:
  ```sh
  cp /app/data/floosy.db /app/data/floosy-backup-$(date +%F).db
  ```
- أو حمّل الملف عبر SFTP من مسار الـ Volume على السيرفر.

**استرجاع:** رجّع ملف `floosy.db` مكانه واعمل Restart.

## 8) الخصوصية (حصري ليك 🔒)

- **لا يوجد تسجيل عام** — الحسابات بتتعمل من جوّه التطبيق فقط (المالك/مدراء الحسابات).
- كل الباسوردات متخزنة **Hash (bcrypt)** — حتى صاحب السيرفر لا يراها.
- محاولات الدخول محدودة (10 / 10 دقايق لكل IP).
- لو الريبو على GitHub لسه **Public** وحابب الخصوصية الكاملة:
  GitHub → الريبو → Settings → Danger Zone → **Change visibility → Private**.
  (Coolify بيكمل Deploy من الريبو الخاص عادي بعد ربطه — وأي Push جديد بيعمل Redeploy تلقائي لو مفعّل Auto Deploy).

## تجربة محلية سريعة (اختياري)

```powershell
$env:JWT_SECRET="<المفتاح>"; $env:ADMIN_USERNAME="حسن"; $env:ADMIN_PASSWORD="<باسورد>"
node server/server.js
# افتح http://localhost:3000
```
