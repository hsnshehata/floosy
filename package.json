{
  "name": "floosy-web",
  "version": "2.1.0",
  "private": true,
  "description": "Floosy frontend CSS build (Tailwind, self-hosted)",
  "scripts": {
    "build:css": "tailwindcss -i ./tw-input.css -o ./tw.css --minify",
    "watch:css": "tailwindcss -i ./tw-input.css -o ./tw.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
